`main.zip`: cli版本

`main_gui.zip`: gui版本（图形化界面）

`aiohttp.zip`: aiohttp的cli版本

**使用步骤**

1. 下载到本地

2. 解压，在同目录下放置 `config.json` 文件以及新建一个 `cache` 文件夹

   - 下载`config.json`后，用记事本打开，填写账号密码后保存在解压后的文件夹中；

   - 在解压后的文件夹里新建一个名为 `cache` 的文件夹。

3. 双击 `.exe` 文件运行即可。
